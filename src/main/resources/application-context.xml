<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:util="http://www.springframework.org/schema/util"
	xmlns:ehcache="http://ehcache-spring-annotations.googlecode.com/svn/schema/ehcache-spring"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.1.xsd
        http://ehcache-spring-annotations.googlecode.com/svn/schema/ehcache-spring http://ehcache-spring-annotations.googlecode.com/svn/schema/ehcache-spring/ehcache-spring-1.1.xsd">

	<import resource="herbcat-deduplication-config.xml"/>
	
	<!--  caching stuff goes here -->
    <ehcache:annotation-driven />
 
    <ehcache:config cache-manager="cacheManager">
        <ehcache:evict-expired-elements interval="60" />
    </ehcache:config>
 
    
	<bean id="cacheManager" class="org.springframework.cache.ehcache.EhCacheManagerFactoryBean"
		p:configLocation="ehcache.xml"/>
	
	<!-- Lucene stuff goes here -->
	
	<!-- Define the Lucene version -->
	<util:constant id="LUCENE_VERSION"
		static-field="org.apache.lucene.util.Version.LUCENE_31" />

	<!-- Set the analyzer, to be used by the IndexWriter -->
	<bean id="luceneAnalyzer" class="org.apache.lucene.analysis.standard.StandardAnalyzer">
		<constructor-arg ref="LUCENE_VERSION" />
	</bean>
	
	<!-- Set the Lucene directory -->
	<!-- also, using the SimpleFSLockFactory -->
	<bean id="luceneFileSystemStore" class="org.apache.lucene.store.FSDirectory"
		factory-method="open">
		<constructor-arg>
			<bean class="java.io.File">
				<constructor-arg ref="lucene_directory"/>
			</bean>
		</constructor-arg>
		<constructor-arg>
			<bean class="org.apache.lucene.store.SimpleFSLockFactory" />
		</constructor-arg>
	</bean>
	
	<!-- Define the IndexWriter -->
	<bean id="indexWriter" class="org.apache.lucene.index.IndexWriter">
		<constructor-arg ref="luceneFileSystemStore" />
		<constructor-arg>
			<bean class="org.apache.lucene.index.IndexWriterConfig">
				<constructor-arg ref="LUCENE_VERSION" />
				<constructor-arg ref="luceneAnalyzer" />
			</bean>
		</constructor-arg>
	</bean>

	
	<!-- This defines a LuceneDataLoader, and passes in some Lucene-specific object 
	references (defined above) and the de-duplication configuration ("config") -->
	<bean id="dataLoader" class="org.kew.shs.dedupl.lucene.LuceneDataLoader"
		init-method="setup"
		p:luceneVersion-ref="LUCENE_VERSION"
		p:indexWriter-ref="indexWriter" 
		p:analyzer-ref="luceneAnalyzer"
		p:directory-ref="luceneFileSystemStore" 
		p:configuration-ref="config"/>

	<bean id="indexSearcher" class="org.apache.lucene.search.IndexSearcher">
		<constructor-arg ref="luceneFileSystemStore" />
	</bean>

	<bean id="queryParser" class="org.apache.lucene.queryParser.QueryParser">
		<constructor-arg ref="LUCENE_VERSION" />
		<constructor-arg value="id" />
		<constructor-arg ref="luceneAnalyzer" />
	</bean>

	<!-- This defines a LuceneDeduplicator, and passes in some Lucene-specific object 
		references (defined above) and the de-duplication configuration ("config") 
		Note that the deduplicator depends on the dataloader. This means that 
		the dataloader class is instantiated and any initialisation is done *before* 
		the instantiation of this class. -->
	<bean id="deduplicator" class="org.kew.shs.dedupl.lucene.LuceneDeduplicator"
		depends-on="dataLoader"
		p:luceneVersion-ref="LUCENE_VERSION" 
		p:indexSearcher-ref="indexSearcher"
		p:indexWriter-ref="indexWriter" 
		p:analyzer-ref="luceneAnalyzer" 
		p:directory-ref="luceneFileSystemStore"
		p:queryParser-ref="queryParser" 
		p:configuration-ref="config"/>

	<!-- This defines a LuceneMatchInvestigator , and passes in some Lucene-specific object 
		references (defined above) and the configuration ("config") -->
	<bean id="matchInvestigator" class="org.kew.shs.dedupl.lucene.LuceneMatchInvestigator"
		p:luceneVersion-ref="LUCENE_VERSION"
		p:indexSearcher-ref="indexSearcher" 
		p:directory-ref="luceneFileSystemStore" 
		p:configuration-ref="config"
		p:queryParser-ref="queryParser"/>

</beans>